import '@testing-library/jest-dom/extend-expect';

describe('visual timer which accurately refects the timer for a session', () => {
  it.skip('check that the coords are correctly drawn', () => {});
});
